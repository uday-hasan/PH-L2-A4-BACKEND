enum USERTYPE {
    ADMIN
    CUSTOMER
    SELLER
}

model User {
    id        String    @id @default(uuid())
    email     String    @unique
    password  String
    userType  USERTYPE
    address   Address[]
    createdAt DateTime  @default(now())
    updatedAt DateTime  @updatedAt
}
